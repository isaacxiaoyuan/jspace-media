# W3 — prompt-length robustness arm (VLP program, 2026-07-21, slinky server)

Media for the Ch. 12 W3 appendix (Notion). Layout:
- `sheets/` — per-prompt contact sheets: EVERY cell of every W3
  experiment (FLUX sever ×5, FLUX clamp ×10 prompt-seeds, clamp_ext grid
  sample, sever_ext seed sweeps ×3 questions, LingBot lateband 5×3,
  LingBot clamp ×5 + s3/s4 ext).
- `frames/` — full-res key cells (noir render-band violation pair,
  alpine tent-deletion pair, market flip-vs-deletion pair, redcar_s21
  x4 viewpoint case study).
- `videos/` — LingBot mp4s (H.264/yuv420p): lateband base/severed ×3
  prompts, clamp flips (night pink→blue @L16_s2, harbor yellow→green
  @L00_s2).
- `figs/` — X4 DINO curves (both models), CMI rel_resolved.
- `prompts/` — ALL 10 long prompt texts + flip metadata (JSON).
Numbers/verdicts: DroPE-Forcing branches flux-jspace/lingbot-jspace/
elt-loop-bmjvp-1.3b @ 2026-07-21 journals.
